<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="colorPrimaryDark">@color/colorPrimaryDark</item>-->
        <!--<item name="colorAccent">@color/colorAccent</item>-->
        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="BlueTheme" parent="AppTheme">
        <item name="themeColor">@color/colorPrimary</item>
    </style>

    <style name="RedTheme" parent="AppTheme">
        <item name="themeColor">@color/red</item>
    </style>

    <style name="YellowTheme" parent="AppTheme">
        <item name="themeColor">@color/smart_tab</item>
    </style>

    <style name="BlackTheme" parent="AppTheme">
        <item name="themeColor">@color/lightBlack</item>
    </style>

    <style name="GreenTheme" parent="AppTheme">
        <item name="themeColor">@color/green</item>
    </style>

    <style name="PurpleTheme" parent="AppTheme">
        <item name="themeColor">@color/purple</item>
    </style>

    <style name="PinkTheme" parent="AppTheme">
        <item name="themeColor">@color/pink</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="BlueTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="BlueTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="AppTheme.TabLayout" parent="Widget.Design.TabLayout">
        <item name="android:background">?colorPrimary</item>
        <item name="tabMinWidth">0dp</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:paddingRight">10dp</item>
        <item name="tabIndicatorColor">@android:color/white</item>
        <item name="tabSelectedTextColor">@android:color/white</item>
        <item name="tabTextAppearance">@style/AppTheme.TabLayout.TextAppearance</item>
    </style>

    <style name="AppTheme.TabLayout.TextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">#6fff</item>
        <item name="android:textAllCaps">false</item>

    </style>
    <style name="CollapsingToolbarTitleStyle"
        parent="@android:style/TextAppearance">
        <item name="android:textColor">@android:color/transparent</item>
    </style>
</resources>
